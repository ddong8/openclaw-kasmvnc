# Nginx 配置示例 - claw.ihasy.com

server {
    listen 80;
    listen [::]:80;
    server_name claw.ihasy.com;

    # 强制跳转 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name claw.ihasy.com;

    # Let's Encrypt SSL 证书路径
    ssl_certificate /etc/letsencrypt/live/claw.ihasy.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/claw.ihasy.com/privkey.pem;

    # SSL 优化配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # 网站根目录
    root /var/www/claw.ihasy.com;
    index index.html;

    # 日志
    access_log /var/log/nginx/claw.ihasy.com.access.log;
    error_log /var/log/nginx/claw.ihasy.com.error.log;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # 主页
    location / {
        try_files $uri $uri/ =404;
    }

    # 脚本文件 - 从 /var/www/openclaw-kasmvnc/ 提供
    location ~ ^/(openclaw-kasmvnc.*\.(sh|ps1))$ {
        alias /var/www/openclaw-kasmvnc/$1;
        add_header Content-Type text/plain;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # 静态资源缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
}
